/**
 * Virtual Resource API
 *
 */

//--------------------------------------------------------------------------------------------------
/**
 * Maximum number of bytes (excluding terminator) in the value of a string type data sample.
 */
//--------------------------------------------------------------------------------------------------
DEFINE MAX_STRING_VALUE_LEN = 50000;

//--------------------------------------------------------------------------------------------------
/**
 * Persist the current Virtual Resources values to disk.
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t PersistValues
(
);

//--------------------------------------------------------------------------------------------------
/**
 * Update the incoming config with VR persisted values
 */
//--------------------------------------------------------------------------------------------------
FUNCTION le_result_t UpdateConfigWithPersistedValues
(
    string incomingConfig[MAX_STRING_VALUE_LEN] IN,///< Incoming Virtual Resources Configuration
    string updatedConfig[MAX_STRING_VALUE_LEN] OUT///< Updated Virtual Resources Configuration
);
